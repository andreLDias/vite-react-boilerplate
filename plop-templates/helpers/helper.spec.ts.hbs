import { {{camelCase name}} } from './{{name}}'

describe('{{camelCase name}}', () => {
  it('should work correctly', () => {
    // Add your test cases here
    const result = {{camelCase name}}('test')

    expect(result).toBe('test')
  })
})
